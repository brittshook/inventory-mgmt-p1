import { render, screen } from "@testing-library/react";
import { ScreenSizeProvider, useScreenSize } from "../ScreenSizeContext";
import "@testing-library/jest-dom";

describe("Screen Size Context", () => {
  test("", () => {});
});
